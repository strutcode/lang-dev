{
  "name": "tizz",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vitest": "^3.1.3"
  },
  "scripts": {
    "cli": "bun run src/cli/index.ts",
    "build": "bun build src/index.ts --outdir=dist --format=cjs,esm --minify",
    "test": "vitest"
  }
}